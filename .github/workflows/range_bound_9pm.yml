name: Update Range Bound 9pm

on:
  workflow_dispatch:
  schedule:
    - cron: '30 15 * * 1-5'   # 9:00 PM IST = 3:30 PM UTC, Mon-Fri

permissions:
  contents: write

concurrency:
  group: data-update-user-profile
  cancel-in-progress: false

jobs:
  update-range-bound:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          ref: user_profile
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r backend/requirements.txt

      - name: Update range bound CSV
        run: python backend/range_bound/update_range_bound_sheets.py

      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add backend/data/
          git diff --cached --quiet && echo "No changes." && exit 0
          git commit -m "Daily range bound update"
          git pull --rebase origin user_profile
          git push origin HEAD:user_profile
